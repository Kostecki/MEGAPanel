notifications:
  email: false
  slack: thunderducks:$SLACK_TOKEN

sudo: required

branches:
  only:
  - master

services:
  - docker
      
script:
  - docker build -t kostecki/megapanel .

after_success:
- echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
- docker push kostecki/megapanel